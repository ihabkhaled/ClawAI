{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "cd ../.. && npm ci",
  "buildCommand": "mkdir -p public && printf '<!doctype html><title>claw-connector-service</title><p>ClawAI API service. Health: /api/v1/health' > public/index.html && cd ../.. && npm run vercel:build:shared && npm run prisma:generate --workspace=claw-connector-service && npm run build --workspace=claw-connector-service",
  "framework": null,
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
