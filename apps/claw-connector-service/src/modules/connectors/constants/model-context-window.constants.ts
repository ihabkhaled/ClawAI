/**
 * Page size for Google's native `GET /v1beta/models`, the list that carries
 * `inputTokenLimit`. One page covers the whole Gemini catalog.
 */
export const GEMINI_NATIVE_MODELS_PAGE_SIZE = 1000;
