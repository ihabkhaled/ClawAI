import { RoutingLabPromptLengthBucket } from '../../../../common/enums';
import { DomainTag } from '../../../../generated/prisma';

/** Every generated single-fault case shares this content — only the injected code varies. */
export const FAULT_SINGLE_DOMAIN = DomainTag.GENERAL;
export const FAULT_SINGLE_LENGTH_BUCKET = RoutingLabPromptLengthBucket.SHORT;
