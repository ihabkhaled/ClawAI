export const COMPARE_INTENT_REGEX =
  /\b(compare|versus|vs\.?|differences? between|pros and cons of|which is better)\b/i;

export const RESEARCH_INTENT_REGEX =
  /\b(find (the )?latest|recent papers|state of the art|research (on|the latest)|summarize the latest|what'?s new in)\b/i;

export const CODE_REVIEW_REGEX =
  /\b(code review|review (this|my) (code|pr|pull request|function|class)|audit (this|my) code|find bugs in)\b/i;
