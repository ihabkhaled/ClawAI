{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "cd ../.. && npm ci",
  "buildCommand": "mkdir -p public && printf '<!doctype html><title>claw-agent-service</title><p>ClawAI API service. Health: /api/v1/health' > public/index.html && cd ../.. && npm run vercel:build:shared && npm run prisma:generate --workspace=claw-agent-service && npm run build --workspace=claw-agent-service",
  "framework": null,
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
