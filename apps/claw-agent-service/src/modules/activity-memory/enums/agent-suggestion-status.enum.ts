/**
 * V2 Stream 05 — must match Prisma `AgentSuggestionStatus` enum.
 */
export enum AgentSuggestionStatusEnum {
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  DISMISSED = 'DISMISSED',
  EXPIRED = 'EXPIRED',
}
