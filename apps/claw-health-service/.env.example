# =============================================================================
# Health Service Configuration (stateless aggregator)
# =============================================================================

# Service port
PORT=4009

# Allowed CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:80,http://localhost
