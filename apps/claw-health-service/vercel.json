{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "cd ../.. && npm ci",
  "buildCommand": "mkdir -p public && printf '<!doctype html><title>claw-health-service</title><p>ClawAI API service. Health: /api/v1/health' > public/index.html && cd ../.. && npm run vercel:build:shared && npm run build --workspace=claw-health-service",
  "framework": null,
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
