// Translation-key identifier for the time-of-day greeting headline. Values
// are the literal i18n keys consumed by `t(...)` in the hero so we avoid a
// separate mapping table.
export enum DashboardGreetingKey {
  MORNING = 'dashboard.heroGreetingMorning',
  AFTERNOON = 'dashboard.heroGreetingAfternoon',
  EVENING = 'dashboard.heroGreetingEvening',
}
