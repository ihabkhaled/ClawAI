// Which size RecordingWaveform renders: the full-height hero view in the
// full-screen recording surface, or the short strip inside its pill bar.
export enum RecordingWaveformVariant {
  Hero = 'hero',
  Compact = 'compact',
}
