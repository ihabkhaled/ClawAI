/** Formats an AI answer can be downloaded as (F2). Values match the server's FileFormat. */
export enum AnswerExportFormat {
  MD = 'MD',
  TXT = 'TXT',
  HTML = 'HTML',
  DOCX = 'DOCX',
  PDF = 'PDF',
}
