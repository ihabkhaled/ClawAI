// Visual variants for the EmptyState primitive.
// - Default: the historical dashed-border card.
// - Compact: tighter spacing for use inside drawers / sidebars / popovers.
// - Page: tall, centered layout for page-level empty states (60vh).
export enum EmptyStateVariant {
  Default = 'default',
  Compact = 'compact',
  Page = 'page',
}
