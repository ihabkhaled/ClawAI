// Visual variants for the design-system CopyButton primitive. These map onto
// the underlying shadcn Button `variant` values so the copy button blends
// into whatever surface it's dropped on.
export enum CopyButtonVariant {
  Default = 'default',
  Ghost = 'ghost',
  Outline = 'outline',
}
