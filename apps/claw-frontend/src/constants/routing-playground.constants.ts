import { RoutingMode } from '@/enums';
import type { RoutingPlaygroundTab } from '@/types';

export const ROUTING_PLAYGROUND_TAB_SEMANTIC: RoutingPlaygroundTab = 'semantic';

export const ROUTING_PLAYGROUND_DEFAULT_MODE: RoutingMode = RoutingMode.AUTO;

export const ROUTING_PLAYGROUND_MAX_MESSAGE_CHARS = 20_000;
