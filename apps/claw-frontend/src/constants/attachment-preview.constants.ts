/** A sent file's own mimeType/filename never change once uploaded. */
export const ATTACHMENT_FILE_META_STALE_MS = 60 * 60 * 1000;

/** How much of a text-like attachment (txt/md/csv/json/…) to show inline before "view full". */
export const ATTACHMENT_TEXT_PREVIEW_MAX_CHARS = 800;
