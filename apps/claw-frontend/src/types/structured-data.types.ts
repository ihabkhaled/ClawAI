export type JsonLdObject = Record<string, unknown>;

export type PublicPageJsonLdInput = {
  name: string;
  description: string;
  canonicalUrl: string;
  language: string;
  lastReviewed: string;
};
