# 08 — Architecture

> SDLC artifact template. Depth scales with risk (see [README](README.md)). Delete
> the guidance blockquotes and fill in real content. For a small change, a few
> lines per required section is fine; a high-risk change fills every prompt.

## Affected services / packages / frontend modules

> Cross-check against .ai/manifests/services.json.

## Data flow (sequence or narrative)

## New/changed events (packages/shared-types EventPattern)

## New/changed endpoints

## Key decisions + trade-offs (link an ADR if material)
