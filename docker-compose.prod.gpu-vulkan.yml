# Intel/generic Vulkan GPU passthrough overlay (prod) for claw-llamacpp-service.
# Auto-loaded by ./scripts/claw.sh --prod when /dev/dri/render* is detected.

services:
  llamacpp-service:
    devices:
      - /dev/dri
    group_add:
      - video
      - render
